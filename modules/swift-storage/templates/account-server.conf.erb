[DEFAULT]
bind_ip = <%= if has_variable?('swift_storage_net_interface') then eval("ipaddress_#{swift_storage_net_interface}") else "0.0.0.0" end %>
workers = 2

[pipeline:main]
pipeline = account-server

[app:account-server]
use = egg:swift#account

[account-replicator]

[account-auditor]

[account-reaper]
